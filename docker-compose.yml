version: "3.8"

services:

  backend:
    build: ./backend
    container_name: trip_backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    depends_on:
      - redis

  frontend:
    build: ./frontend
    container_name: trip_frontend
    ports:
      - "3000:80"
    env_file:
      - ./frontend/.env
    depends_on:
      - backend

  redis:
    image: redis
    container_name: redis
    ports:
      - "6379:6379"